import { AppRegistry } from 'react-native';
import App from './App';
import SearchableList from './collection/SearchableList'

AppRegistry.registerComponent('rulebook', () => App);
AppRegistry.registerComponent('SearchableList', () => SearchableList)